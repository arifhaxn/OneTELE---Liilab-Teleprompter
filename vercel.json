{
  "framework": null,
  "installCommand": "git config --global --add safe.directory '*' && git clone https://github.com/flutter/flutter.git --depth 1 -b 3.41.6",
  "buildCommand": "flutter/bin/flutter config --enable-web && flutter/bin/flutter build web --release",
  "outputDirectory": "build/web",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
